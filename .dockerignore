# dependencies & build output (reinstalled/rebuilt inside the image)
node_modules
.next
out
build

# version control & editors
.git
.gitignore
.idea
.vscode
.DS_Store

# project docs, archived design handoffs & the standalone client templates
# (none of it is needed in the image)
docs
sites
.github
README.md

# local verification artifacts
.playwright-mcp
*.png

# docker / ci
Dockerfile
docker-compose.yml
.dockerignore

# logs & local env — .env holds real secrets and must NEVER enter the build
# context (the builder stage does `COPY . .`); runtime env comes from compose.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env.*
!.env.example
