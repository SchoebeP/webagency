# dependencies & build output (reinstalled/rebuilt inside the image)
node_modules
.next
out
build

# version control & editors
.git
.gitignore
.idea
.vscode
.DS_Store

# project docs & the design-handoff reference (not needed in the image)
docs
design_handoff_site_vitrine
README.md

# local verification artifacts
.playwright-mcp
*.png

# docker / ci
Dockerfile
docker-compose.yml
.dockerignore

# logs & local env
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env*.local
